spring.application.name = product-service

# Listen on this port
server.port=3010
eureka.client.serviceUrl.defaultZone=http://localhost:4000/eureka/

# Switch off logging for the discovery client of eureka
logging.level.com.netflix.discovery.DiscoveryClient: WARN
